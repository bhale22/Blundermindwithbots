{
  "name": "blundermind",
  "version": "1.0.0",
  "description": "Stop getting blundermined. Board vision training for novice chess players.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test \"test/**/*.test.js\"",
    "test:browser": "node --test --test-concurrency=1 \"test/browser/*.spec.js\"",
    "test:all": "npm test && npm run test:browser"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
